# Existing-codebase briefing fixture

An existing project contains `README.md`, `package.json`, and source files. A `/brief refresh` should record these as `[inferred]` evidence rather than silently accepting them as requirements.
