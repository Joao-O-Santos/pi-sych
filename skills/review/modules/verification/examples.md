# Verification examples

Input: a feature passes unit tests but has no package-load check.

Output: report the passed test, identify the missing integration evidence, and run or propose the smallest relevant check.
