# Testing examples

Input: a new parser behavior is requested.

Output: have an independent worker specify failing accepted cases, review them, implement minimally, then run native checks without weakening those cases.
