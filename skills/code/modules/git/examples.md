# Git examples

Input: a contributor has unrelated formatting and behavior changes together.

Output: separate them into reviewable commits, state checks actually run, and wait for authorization before any remote operation.
