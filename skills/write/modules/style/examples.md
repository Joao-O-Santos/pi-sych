# Style examples

Input: a Portuguese draft uses `você`, a gerund-heavy progressive, and `arquivo`.

Output: classify each as pt-BR usage, register choice, valid variant, or error before proposing pt-PT changes.
