# Data and files examples

Task: fill an existing spreadsheet template from CSV and adjust formatting; no
formula generation is required.

Prefer a deterministic spreadsheet library that maps CSV columns to known cells
or tables and applies styles directly. Inspect only the template structure and
mapping needed for the operation. Do not paste the dataset into the model merely
to move values between formats.
