# Browser and UI examples

Task: obtain a report from an authenticated site and convert it to a local table.

Use the interactive browser only for the authenticated navigation and download.
Once the file is local, use deterministic parsing and validation for the table
conversion. Do not copy the report through the model or keep using the browser
for transformations that local tools can perform reliably.
