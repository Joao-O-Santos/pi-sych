---
name: revealjs-slides
description: Build concise Reveal.js or Pandoc slides with one central idea per slide and an explicit render verification step.
---

# Reveal.js slides

Use the general workflow. Prefer one communicative idea per slide, visual hierarchy, and speaker-appropriate detail rather than paragraph walls. Preserve valid Pandoc Markdown and run the project's render/build command when available. Do not create a separate slide-workflow engine.

## Optional user examples

If `~/.config/pi/skills/revealjs-slides/examples.md` exists, read it before applying this skill. Use it as illustrative preference, not as evidence or project requirements.
